/**
 * The snap origin to use.
 * Will default to the local hosted snap.
 * Note: This package may need to be installed separately when available
 */
export const defaultSnapOrigin = `npm:@qubic-lib/qubic-mm-snap`;
