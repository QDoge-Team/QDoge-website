/**
 * Backend base URL. Point at a local dev server ("http://192.168.x.x:3000")
 * while testing on a device, or the production site once deployed.
 */
export const API_BASE = 'http://localhost:3000/api/move-to-earn';
